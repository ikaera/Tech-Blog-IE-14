<!-- We are tracking the number of times we visit this homepage -->

<ul class='blog-list'>
  {{#each blogs as |blog|}}
    <li>
      <article class='blog'>
        <section class='entire-blog'>
          <div class='info'>
            <section class='blog-name'>
              <a href='/blog/{{blog.id}}' class='links'>{{blog.title}}</a>
            </section>
            <section class='created-by'>
              <p id='comment-subtitle'>
                Posted by
                <b>{{blog.usertech.username}} </b>
                on
                <b>{{formatDate blog.date_created}}</b>
              </p>
            </section>
          </div>
          <section class='content-div'>
            <p>{{blog.description}}</p>
          </section>
        </section>
      </article>
      {{!-- {{blog.title}} --}}
    </li>
  {{/each}}
  <br />
</ul>
{{!-- <ul class='visit-count'>
  <li>
    Webpage was visited by
    {{countVisit}}
    times
  </li>

  <li>Number of daily unique sessions which hit the server:
    {{numberOfDailyUniqueSessions}}</li>

  <li>Number of daily unique ip addresses which hit the server:
    {{numberOfDailyUniqueIpAddresses}}</li>

  <li>Number of daily HTTP requests received by the server is:
    {{numberOfDailyRequests}}</li>

</ul> --}}

{{! <divclass="row mb-4 project">
  <h1>Hello</h1>
</div> }}