<div class='comments'>
  {{#each this}}
    <section class='comment'>
      <div class='meta'>
        {{usertech.username}}: on
        {{formatDate date_created}}
      </div>
      <div class='text'>
        {{comment_description}}
      </div>
    </section>
  {{/each}}
</div>